# 引き継ぎ書 — Threads自動投稿システム（@mirei_fondly）

このシステムを**別の人が引き継いで運用・管理する**ための完全ガイドです。
プログラミングの知識がなくても、この通りに読めば「何が起きているか」「自分は何をすればいいか」
「困ったらどうするか」が分かるようにしてあります。

> - **初めてゼロから別アカウントで立ち上げたい人** → `SETUP.md`（セットアップ手順）
> - **ファイルの早見表だけ見たい人** → `システム解説.md`（地図）
> - **このファイル** → 引き継ぎ（運用者が日々やること・仕組み・ハマりどころ・直し方）

最終更新: 2026-09-10

---

## 0. まず結論：あなた（引き継ぐ人）がやることは少ない

このシステムは **GitHub のクラウド上で全自動** で動きます。パソコンを常に開いておく必要も、
毎日操作する必要もありません。あなたの仕事は主に **「見守る」＋「たまに方針を変える」** だけです。

| 頻度 | やること | 所要 |
|---|---|---|
| **毎日** | 基本なし。投稿はThreadsアプリで眺めて、変な投稿があれば手で消す程度 | 数分 |
| **週1** | GitHubの「Actions」タブが緑（成功）か軽く確認。フォロワー数の推移を見る | 5分 |
| **随時** | 「口調を変えたい」「時刻を変えたい」等 → Claude（対話AI）に頼む | — |
| **節目** | フォロワーが150〜200人を超えたら販売モードに切替（後述） | — |
| **月1** | トークンは自動延命されるが、`refresh.yml`が緑か一応確認 | 1分 |

**つまり「放っておいても回る」。** 触るのは方針を変えたいときだけです。

---

## 1. これは何をするシステムか（3分で理解する）

### ひとことで
**恋愛系YouTube動画の内容を素材に、@mirei_fondly（キャラ「みれい」）の投稿を毎日自動で作って、
Threadsに自動で連投し、反応を測って自分で改善し続ける**システムです。

### 3つの動き
1. **毎日：下書きを作る → 決まった時刻に投稿する**（メインの自動投稿）
2. **週1：自分の投稿の成績を測り、伸びない「型」を新しい型と入れ替える**（自動で賢くなる）
3. **月1：Threadsの「鍵（トークン）」を自動で延命する**（止まらないように）

### 全体像（絵）
```
  ┌─ 毎日3:00 ────────────────────────────────┐
  │  YouTube字幕  ＋  型(kata)  ＋  みれいの人格   │   ← generate.py（下書き工場）
  │        ↓ Claude(AI)が連投の文章を執筆          │
  │     posts/queue.json（下書き在庫・約3日分）     │
  └───────────────────────────────────────────┘
                      ↓
  ┌─ 毎日 9:00 / 15:00 / 21:00 ──────────────┐
  │  在庫から今の枠の連投を取り出して投稿          │   ← publish.py（投稿係）
  │  1本目→返信で2本目→…と「連投」でつなぐ          │
  │  最後にCTA（フォロー誘導 or note誘導）          │
  └───────────────────────────────────────────┘
                      ↓
  ┌─ 毎週月曜2:00 ───────────────────────────┐
  │  各投稿の表示数・いいねを測る                  │   ← measure.py + optimize.py
  │  → 伸びない「型」を引退、新しい型をリサーチ追加  │   （1週1個だけ入れ替え・慎重に）
  └───────────────────────────────────────────┘
```

### 「型（かた）」という考え方（重要）
バズっている投稿には**共通の"骨組み"**があります（例：「意外な事実を予告→理由を列挙→まとめ」）。
この骨組みだけを抜き出したものを **「型」** と呼び、`kata-library/` に貯めています。
中身（テーマ・具体例）はYouTube素材から、**骨組みだけ型から**使って投稿を作ります。
だから「毎回ゼロから書く」より速く、かつ「当たる構造」を再利用できます。

### なぜGitHubが必要か
GitHub は「コードの保管庫」であり、同時に **「無料のタイマー実行サーバー（GitHub Actions）」** です。
決まった時刻にプログラムを自動で走らせてくれるので、あなたのPCは不要です。

---

## 2. いつ・何が動くか（自動タイマーの一覧）

自動実行の正体は `.github/workflows/` にある6つの設定ファイルです。

| ファイル | タイミング | 何をする |
|---|---|---|
| `publish.yml` | 投稿時刻の前後だけ15分おき | 予定時刻を過ぎた枠の連投を投稿する |
| `generate.yml` | 毎日 3:00（JST） | 約3日分の連投下書きを作って在庫に貯める |
| `optimize.yml` | 毎週月曜 2:00（JST） | 成績を測り、伸びない型を新型と入れ替える |
| `refresh.yml` | 毎月1日 | Threadsトークンを延命（失効防止） |
| `get-token.yml` | 手動（ボタン） | Threadsトークンを取得・保存（初回用） |
| `init-account.yml` | 手動（ボタン） | 前運用者のデータを空にする（初回用） |

> GitHubの時計は世界標準時(UTC)ですが、中の設定で日本時間(JST)に直してあります。
> **投稿時刻を変えたいときは `config/slots.json` と `publish.yml` の `cron` の両方**を直す必要があります
> （片方だけだとズレる）。これはClaudeに頼めば両方そろえてくれます。

---

## 3. 全ファイル解説（フォルダごと）

記号: ★＝運用でよく触る / ⚙️＝プログラム本体（普段触らない） / 🔒＝自動更新（手で触らない）

### 🧠 `src/` — システムの本体（⚙️ 普段触らない）
| ファイル | 役割 |
|---|---|
| `generate.py` | **下書き工場**。YouTube字幕＋型＋人格で連投を作り、CTAも付ける。絵文字チェックもここ |
| `publish.py` | **投稿係**。連投を返信でつないで投稿。途中で失敗しても次回続きから再開する |
| `measure.py` | 自分の投稿の成績（表示数・いいね等）を型ごとに集計する |
| `optimize.py` | 成績で伸びない型を引退させ、新しい型をリサーチして入れ替える |
| `refresh_token.py` | トークンを延命し、GitHubの金庫(Secret)を更新する |
| `get_token.py` | トークンを取得する（初回セットアップ用） |
| `slots.py` | 「今どの投稿枠か」の時刻計算 |
| `threads_common.py` | 共通部品（設定読込・文字数カウント・通知・時刻処理） |

### 🔧 `scripts/` — 補助スクリプト（⚙️）
| ファイル | 役割 |
|---|---|
| `fetch_youtube.py` | YouTubeの人気動画選定・**字幕取得**（Apify経由。ハマりどころ参照） |
| `fetch_threads.py` | Threadsのバズ投稿収集（Apify経由。型リサーチ用） |
| `check_length.py` | 投稿の機械チェック（500字・記号など） |
| `init_account.py` | 新アカウント用にデータを空に戻す |
| `common.py` | scripts用の共通部品 |

### ★ `config/` — 設定（よく触る）
| ファイル | 役割 | 触る頻度 |
|---|---|---|
| `account.json` | ハンドル・表示名・ジャンル・note誘導先・ベンチマーク・**CTAモード**・ピン留め投稿 | 高 |
| `slots.json` | 投稿する時刻・各枠のテーマ・トーン | 中 |
| `ng_words.json` | 使ってほしくない表現のリスト ※**現状コード未接続**（下の注意） | 低 |
| `account.example.json` | `account.json`の見本（触らない） | — |

> ⚠️ **`ng_words.json` は現在、生成・検証のコードにつながっていません**（リストがあるだけ）。
> 禁止ワードを本当に弾きたい場合は「ng_wordsを生成チェックに組み込んで」とClaudeに頼めば実装できます。

### ★ `assets/` — キャラと文面（“味”を決める）
| ファイル | 役割 |
|---|---|
| `persona.md` | **みれいの人格設定（声・口調・性格）**。投稿の雰囲気そのもの。絵文字禁止もここ |
| `cta.md` | CTA（誘導文）の参考例。※丸写しはせず構成だけ参考にする |
| `youtube-channels.txt` | 素材にするYouTubeチャンネル（1行に1つ。追加・削除OK） |

### 📚 `kata-library/` — 型ライブラリ
| ファイル | 役割 |
|---|---|
| `<型名>.md` | 1ファイル＝1つの型（バズ構造の骨組み）。今は5つ |
| `INDEX.md` | 型の一覧表（`optimize`が自動更新） |
| `_common-modules.md` | 全投稿共通の文体・出力ルール（**絵文字禁止・人間っぽく書く**もここ） |

> 型は `optimize.yml` が週1で自動入れ替え。引退した型は消えず `active: false` になるだけ。
> 手で追加したいときはClaudeに「新しい型を足して」と頼む。

### 💾 `posts/` — 状態データ（🔒 システムが自動で書く。手で触らない）
| ファイル | 役割 |
|---|---|
| `queue.json` | 下書き在庫（これから投稿する連投） |
| `state.json` | 各枠の投稿状況（二重投稿を防ぐ） |
| `processed.json` | 投稿済みID台帳（二重投稿を防ぐ） |
| `used_videos.json` | 使用済みYouTube動画（素材の重複を防ぐ） |
| `performance.json` | 型ごとの成績（`measure`が出力。**ここで成績が見られる**） |
| `token_meta.json` | トークンの有効期限メモ |

### 🆘 `fallback/` — 保険
| ファイル | 役割 |
|---|---|
| `evergreen.json` | 下書き生成が全部失敗したときに出す、いつ出しても大丈夫な定型投稿 |

### 📖 ドキュメント・その他
| ファイル | 役割 |
|---|---|
| `SETUP.md` | ゼロから立ち上げる完全手順（初心者向け） |
| `システム解説.md` | ファイルの早見表（地図） |
| `引き継ぎ書.md` | このファイル |
| `CLAUDE.md` | システム説明＋AIアシスタント向けの案内（Claudeが最初に読む） |
| `README.md` | 概要 |
| `requirements.txt` | 必要なPythonライブラリ一覧（GitHubが自動で入れる） |
| `.env.example` | 鍵の見本。`.env`（自分のPC内だけの鍵）は作る人が用意。GitHubには上げない |
| `.claude/skills/` | 対話AI用の作業手順（型登録/執筆/一覧）。自動投稿には未使用 |
| `index.html` | このシステムと無関係の置き土産。無視/削除してOK |

---

## 4. 1日〜1週間で実際に起きること（時系列）

```
【毎日 3:00】generate.py が動く
  ・在庫(queue.json)が3日分を切っていたら、足りない枠ぶんだけ下書きを作る
  ・YouTubeチャンネルから未使用の人気動画を選び、字幕を取得（Apify経由）
  ・型をローテーションで選び、みれいの人格で連投を執筆
  ・500字・絵文字なし・重複なし等を機械チェック（ダメなら最大3回まで作り直し）
  ・CTA（大半フォロー誘導／4回に1回note誘導）を付けて queue.json に保存

【毎日 9:00 / 15:00 / 21:00】publish.py が動く（実際は前後の時間帯に15分おき実行）
  ・その枠の連投をqueueから取り出し、1本ずつThreadsへ投稿
  ・2本目以降は前の投稿への「返信」としてつなぐ＝連投
  ・1本投稿するごとに記録。途中で失敗しても次回に続きから再開
  ・最後の投稿にCTA（note回はプロフのピン留め投稿を引用）

【毎週月曜 2:00】optimize.py が動く
  ・measure.py が直近の投稿の表示数・いいねを型ごとに集計（performance.json）
  ・データが十分たまった型のうち、明らかに伸びない1つを引退(active:false)
  ・ベンチマークアカウントのバズを調べ、新しい型を1つ作って追加（1週1入れ替え・慎重）

【毎月 1日】refresh.py が動く
  ・Threadsトークンを延命して、GitHubの金庫を更新（失効で止まるのを防ぐ）
```

---

## 5. 「〜したい」逆引き表（Claudeへの頼み方つき）

基本の直し方は **「Claude Code でこのリポジトリを開いて、やりたいことを日本語で頼む」** だけです。
Claudeがこの引き継ぎ書とCLAUDE.mdを読んで、正しいファイルを直し、GitHubへ反映してくれます。

| やりたいこと | 触る場所 | Claudeへの頼み方の例 |
|---|---|---|
| キャラの口調・性格を変える | `assets/persona.md` | 「みれいの口調をもっと〇〇にして」 |
| ジャンル・note誘導先・ハンドルを変える | `config/account.json` | 「note誘導先を新しいURLに変えて」 |
| 投稿の時刻を変える | `config/slots.json` ＋ `publish.yml` | 「投稿を8時・12時・20時に変えて」 |
| 各枠のテーマ・トーンを変える | `config/slots.json` | 「夜の枠をもっと癒やし系に」 |
| 素材のYouTubeチャンネルを変える | `assets/youtube-channels.txt` | 「このチャンネルを素材に追加して」 |
| CTA（誘導）の中身を変える | `src/generate.py` のCTA指示＋`assets/cta.md` | 「フォロー誘導の言い方を変えて」 |
| **フォロー重視→販売(note)重視に切替** | `config/account.json` の `cta_mode` | 「販売モード（note誘導）に戻して」 |
| リサーチ元（お手本）を変える | `config/account.json` の `benchmark_accounts` | 「お手本アカウントに@〇〇を追加して」 |
| 型を手で足す／止める | `kata-library/` | 「新しい型を足して」「この型を止めて」 |
| 最適化の慎重さを変える | `optimize.yml` の環境変数 | 「型の入れ替えをもっと慎重に」 |
| 生成・投稿の仕組みを変える | `src/generate.py` / `src/publish.py` | （本体。必ず結果を確認） |

---

## 6. 重要な運用判断（この2つは覚えておく）

### (A) CTAモード：いまは「フォロー重視」
`config/account.json` の `cta_mode` で切り替わります。

- **`follow`（今これ／育成期）**：投稿の大半を「フォローして・保存して」という**フォロー獲得**に使い、
  4回に1回だけnote（商材）へ誘導します。
- **`note`（販売期）**：毎回noteへ誘導します。

**なぜ今フォロー重視か**：リーチ（表示数）は強い（数千〜数万）のに、フォロワーが少なく
（例：52人）、見た人がフォローに変わっていないのが課題だったため、まず母数を増やす作戦です。

**いつ切り替えるか**：**フォロワーが150〜200人を超えたら** `note`（販売期）に戻します。
Claudeに「販売モードに戻して」と頼めば、設定変更＋在庫の作り直しまでやってくれます。

### (B) 絵文字は使わない・文章は人間っぽく（2026-09-10の方針）
インプレッションが落ちてきたため、**AI量産っぽさ・スパム感を消す**方針に変えました。
- 絵文字は一切使わない（`persona.md`・`_common-modules.md`・`generate.py`の3か所で禁止＋自動チェック）。
- 文の緩急をつける／整いすぎた対句を避ける／決まり文句に頼らない、等のルールを入れてあります。

**この方針は戻さないでください**（元に戻すとスパム判定のリスクが上がります）。

---

## 7. 鍵（GitHub Secrets）の一覧

GitHubの「Settings → Secrets and variables → Actions」に保管されている鍵です。
**中身は誰にも見せない。** 普段いじる必要はありません（切れたときだけ再設定）。

| Secret名 | 用途 |
|---|---|
| `THREADS_USER_ID` | 投稿先のThreadsアカウントID |
| `THREADS_ACCESS_TOKEN` | Threadsへ投稿するための鍵（約60日で失効→自動延命） |
| `THREADS_APP_ID` / `THREADS_APP_SECRET` / `THREADS_REDIRECT_URI` | トークン取得・延命に使う（Meta側アプリの情報） |
| `ANTHROPIC_API_KEY` | Claude（文章を書くAI）の鍵 |
| `YOUTUBE_API_KEY` | YouTube動画の検索に使う |
| `APIFY_TOKEN` | YouTube字幕取得・Threadsバズ収集に使う |
| `GH_PAT` | システムが自分でGitHubにファイルを書き戻すための鍵 |
| `NOTIFY_WEBHOOK_URL` | （任意）失敗通知の送り先（Slack/Discord等） |

> トークンの有効期限は `posts/token_meta.json` に記録されています（現在の失効予定：**2026-10-31**）。
> 毎月1日の `refresh.yml` が自動で延命するので、基本は放置でOKです。

---

## 8. ハマりどころ（これを知らないと詰まる）

過去に実際にハマって解決した、**仕様上の落とし穴**です。Claudeに直してもらう時の前提知識。

1. **連投（返信でつなぐ）には特別な権限が要る。**
   Threadsの返信投稿には `threads_manage_replies` 権限が必須。これが無いと返信作成が
   `HTTP 500 unknown error` になる（普通の単発投稿は成功するので原因が分かりにくい）。
   今のトークンはこの権限付きで取得済み。トークンを取り直すときは権限を外さないこと。

2. **YouTube字幕はGitHub上（クラウド）から直接取れない。**
   YouTubeがデータセンターのIPをブロックするため。なので **Apify経由**で字幕を取っています
   （`APIFY_TOKEN` が必要）。「ローカルPCでは取れるのにGitHubだと全部失敗」はこれが原因。

3. **成績測定には別の権限が要る。**
   投稿のインサイト取得には `threads_manage_insights` 権限が必須。無いとInsightsが権限エラー。
   これも取得済み。

4. **絵文字は自動で弾く。**
   万一、生成文に絵文字が混じると `generate.py` の検証が検知して作り直します（安全網）。

5. **Threadsの一覧API（投稿直後）は反映が遅い。**
   投稿できたかの確認に使えない（投稿済みでも返ってこないことがある）。確認は**Threadsアプリで目視**。

6. **GitHubへの書き戻しは競合に注意。**
   システムも自分でファイルを書き戻すので、手元から変更を送るときは必ず
   「最新を取り込んでから送る（fetch→rebase→push）」。ここはClaudeがやってくれる。

---

## 9. トラブル対応（症状 → 対処）

まず見る場所は **GitHubの「Actions」タブ**（各自動実行の成功＝緑／失敗＝赤とログが見られる）。

| 症状 | 考えられる原因 | 対処 |
|---|---|---|
| 投稿が止まった | トークン失効／投稿枠の在庫切れ | Actionsで`publish`のログを見る。赤ならClaudeにログを渡す |
| 同じ枠が「在庫切れ(missing)」 | `generate`が失敗して下書きが無い | `generate`のログを確認。字幕取得(Apify)失敗が多い |
| 変な投稿・絵文字入りが出た | 方針変更前の在庫が残っていた | Threadsアプリで手で削除＋Claudeに「在庫を作り直して」 |
| 連投が返信でつながらない | `threads_manage_replies`権限が無いトークン | トークンを権限付きで取り直す（`get-token.yml`） |
| 字幕が全部取れない | Apifyの残高切れ／`APIFY_TOKEN`不正 | Apifyの利用状況を確認、鍵を再設定 |
| AIが書かない(生成ゼロ) | `ANTHROPIC_API_KEY`切れ／残高切れ | Anthropicの残高・鍵を確認 |
| トークンが切れそう | `refresh.yml`が失敗していた | 手動で`refresh.yml`を実行、ダメなら`get-token.yml`で取り直す |

**原則：困ったら「GitHubのActionsの赤い実行のログ」をコピーしてClaudeに渡す。** 原因と直し方を教えてくれます。

---

## 10. 運用者が手を動かす数少ない操作

### (a) Threadsアプリで変な投稿を消す
自動投稿は人のチェックを通りません。内容に問題があれば**Threadsアプリ上で手で削除**してください
（システム側の状態には影響しません）。

### (b) GitHubのActionsで様子を見る
リポジトリの「**Actions**」タブ → 各ワークフローの実行履歴。緑＝成功、赤＝失敗。
赤をクリックするとログが読めます。

### (c) 手動で実行する（ボタン）
Actionsタブで各ワークフローを選び「**Run workflow**」。`publish`をドライラン（投稿せず確認）で
試すこともできます。

### (d) 設定変更はClaudeに頼む
ファイルを直接編集できる人はしてもいいですが、**基本はClaudeに日本語で頼むのが安全**です。
Claudeが正しい場所を直し、GitHubへ反映し、必要なら在庫を作り直します。

---

## 11. Claude Code を使った変更の流れ（技術メモ・任意）

手元のPCにリポジトリを置いてClaude Codeで作業する場合の作法です（Claudeが自動でやる内容）。

- 変更前に必ず最新を取り込む：`git fetch` → `git rebase origin/main`
  （システムが自動で書き戻すため、やらないと競合する）。
- **下書き在庫(queue.json)を作り直すとき**は、事故防止のため一時的に
  `publish.yml` / `generate.yml` / `optimize.yml` を**止めてから**作業し、終わったら**戻す**。
  （GitHub APIでワークフローをdisable→enable。Claudeが手順を知っている。）
- 変更を送ったら **必ずActionsのログと実際の投稿で結果を確認**（本番アカウント直結のため）。

---

## 12. 用語集

| 用語 | 意味 |
|---|---|
| 連投（スレッド） | 1本目に返信を重ねて続ける投稿の束。Threadsの基本形 |
| 型（kata） | バズ投稿から抜き出した"骨組み"。中身は素材から、構造だけ型から |
| CTA | Call To Action＝投稿末尾の誘導（フォローして／noteを読んで 等） |
| ペルソナ(persona) | みれいの人格・声の設定 |
| トークン | Threadsへ投稿するための鍵。約60日で失効→毎月自動延命 |
| ベンチマーク | お手本にするThreadsアカウント。型リサーチの対象 |
| GitHub Actions | GitHubのクラウド上で決まった時刻にプログラムを走らせる仕組み |
| Secrets | GitHubに安全に保管する鍵（APIキー等） |
| Apify | Webからデータを取る外部サービス。ここでは字幕取得とバズ収集に使用 |
| queue（キュー） | 下書き在庫。これから投稿する連投のリスト |

---

## 困ったときの最終手段

1. **GitHubの「Actions」タブ**で赤い実行のログを見る → コピーしてClaudeに渡す
2. **Threadsアプリ**で投稿を目視・手で削除
3. **このファイルと `CLAUDE.md` をClaudeに読ませて**「〇〇したい／〇〇で困っている」と頼む

これだけ押さえれば、プログラミングが分からなくても運用を引き継げます。
