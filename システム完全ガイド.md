# システム完全ガイド — Threads自動投稿テンプレート

このリポジトリは、**1つのThreadsアカウントを丸ごと全自動で運用するための再利用可能なテンプレート**です。
ジャンル・キャラ・投稿時刻・誘導先を設定ファイルで決めるだけで、毎日AIが投稿を作って連投し、
反応を測って自分で改善し続けます。**プログラミングの知識がなくても運用できる**ように書いています。

> - **ゼロから立ち上げる手順** → `SETUP.md`（ボタン操作だけで完結・PC不要）
> - **ファイルの短い早見表** → `システム解説.md`（地図）
> - **このファイル** → 仕組み・全ファイル・自分仕様にする方法・運用・トラブル対応（完全ガイド）
> - **Webページ版**（スマホで読みやすい）もあります。運用者に渡すときはそちらのリンクが便利です。

最終更新: 2026-09-10

---

## 0. まず結論：運用の手間は少ない

セットアップさえ済めば、あとは **GitHub のクラウド上で全自動** で動きます。
PCを開きっぱなしにする必要も、毎日操作する必要もありません。運用者の仕事は主に
**「見守る」＋「たまに方針を変える」** だけです。

| 頻度 | やること | 所要 |
|---|---|---|
| **毎日** | 基本なし。Threadsアプリで投稿を眺め、変な投稿があれば手で消す程度 | 数分 |
| **週1** | GitHubの「Actions」タブが緑（成功）か軽く確認。フォロワー数の推移を見る | 5分 |
| **随時** | 「口調を変えたい」「時刻を変えたい」等 → Claude（対話AI）に頼む | — |
| **節目** | フォロワーが育ってきたらCTAを育成→販売モードへ切替（→ 7章） | — |
| **月1** | トークンは自動延命される。`refresh.yml`が緑か一応確認 | 1分 |

**放っておいても回る。** 触るのは方針を変えたいときだけです。

---

## 1. これは何か（テンプレートの思想）

### ひとことで
ある発信アカウントを1つ、**全自動で運用するためのテンプレート**です。「素材（YouTube動画）」を仕入れ、
「型（バズの骨組み）」と「キャラ設定」を使ってAIが投稿を書き、決まった時刻にThreadsへ連投し、
反応を測って自分で改善し続けます。

### テンプレートである、という考え方（重要）
**仕組み（プログラム）は全員共通で、アカウント固有の部分だけを設定ファイルに外出し**してあります。
だからテンプレートを複製して数個の設定ファイルを書き換えるだけで、まったく別のジャンル・別キャラの
自分のアカウントを動かせます。**1つの複製（リポジトリ）＝ 1つのアカウント**。別アカウントをもう1つ
回したいなら、テンプレートからもう1つ複製を作るだけ。→ 自分仕様にする手順は **2章**。

### 3つの動き
1. **毎日**：下書きを作る → 決まった時刻に投稿する（メインの自動投稿）
2. **週1**：投稿の成績を測り、伸びない「型」を新しい型と入れ替える（自動で賢くなる）
3. **月1**：Threadsの「鍵（トークン）」を自動で延命する（止まらないように）

### 全体像
```
  ┌─ 毎日3:00 ──────────────────────────────────┐
  │  YouTube字幕  ＋  型(kata)  ＋  キャラの人格     │   ← generate.py（下書き工場）
  │        ↓ Claude(AI)が連投の文章を執筆            │
  │     posts/queue.json（下書き在庫・数日分）        │
  └────────────────────────────────────────────┘
                      ↓
  ┌─ 投稿枠ごと（既定 9:00 / 15:00 / 21:00）────┐
  │  在庫から今の枠の連投を取り出して投稿           │   ← publish.py（投稿係）
  │  1本目→返信で2本目→…と「連投」でつなぐ           │
  │  最後にCTA（誘導文）                            │
  └────────────────────────────────────────────┘
                      ↓
  ┌─ 毎週月曜2:00 ────────────────────────────┐
  │  各投稿の表示数・いいねを測る                   │   ← measure.py + optimize.py
  │  → 伸びない「型」を引退、新しい型をリサーチ追加   │   （1週1個だけ入れ替え・慎重に）
  └────────────────────────────────────────────┘
```

### 「型（かた）」とは
バズ投稿には**共通の"骨組み"**があります（例：意外な事実を予告→理由を列挙→まとめ）。
この骨組みだけを抜き出したものが「型」で、`kata-library/` に貯めています。
**中身（テーマ・具体例）は素材から、骨組みだけ型から**使うので、速く・当たる構造を再利用できます。
ジャンルに合わせて型を足す／入れ替えることも可能です。

### なぜGitHubが必要か
GitHub は「コードの保管庫」であり、同時に **無料のタイマー実行サーバー（GitHub Actions）** です。
決まった時刻にプログラムを自動で走らせてくれるので、運用者のPCは不要です。

---

## 2. このテンプレを「自分のアカウント」にする

完全な手順は `SETUP.md`（初心者向け・ボタン操作だけで完結）にあります。ここでは
「どこを変えれば自分仕様になるか」の地図を示します。AIに手伝ってもらう場合は、複製した
リポジトリを Claude に開かせ `CLAUDE.md` を読ませれば、以下を案内・実装してくれます。

### ① アカウント固有の設定は、主に4つのファイル
| ファイル | ここで決めるもの |
|---|---|
| `config/account.json` | ハンドル・表示名・ジャンル・誘導先リンク（note等）・お手本アカウント・CTAモード・ピン留め投稿 |
| `assets/persona.md` | キャラクターの人格・声・口調（投稿の雰囲気そのもの） |
| `config/slots.json` | 投稿する時刻と、各枠のテーマ・トーン |
| `assets/youtube-channels.txt` | 素材にするYouTubeチャンネル（1行に1つ） |

### ② 自分の「鍵」を用意して GitHub の金庫に入れる
各サービスで鍵（APIキー・トークン）を取得し、GitHubの Secrets に登録します（→ 一覧は 7章、
取得手順は `SETUP.md`）。Threads・Anthropic(Claude)・YouTube・Apify・GitHub の鍵が必要です。
**鍵は各運用者が自分のぶんを用意**します（他人と共有しない）。

### ③ 初回だけ動かす「手動ワークフロー」
- `get-token.yml` … Threadsのトークンを取得・保存する（GitHubのボタンで実行）。
- `init-account.yml` … テンプレに残っている前運用者のデータ（在庫・実績）を空にして、まっさらから始める。

### ④ 型はジャンルに合わせて育てる
テンプレートには最初からいくつかの型が入っています。自分のジャンルに合わせて、Claudeに
「このジャンルの型を足して」と頼んで追加できます。以後は週1の自動改善（optimize）が
成績を見て育ててくれます。

> **まとめ**：プログラム本体（`src/`）は触りません。**上の設定ファイル＋鍵**を自分用にするだけで、
> テンプレートがあなたのアカウント専用のシステムになります。

---

## 3. いつ・何が動くか（自動タイマー）

自動実行の正体は `.github/workflows/` にある6つの設定ファイルです。

| ファイル | タイミング | 何をする |
|---|---|---|
| `publish.yml` | 投稿時刻の前後（15分おき） | 予定時刻を過ぎた枠の連投を投稿する |
| `generate.yml` | 毎日 3:00（JST） | 数日分の連投下書きを作って在庫に貯める |
| `optimize.yml` | 毎週月曜 2:00（JST） | 成績を測り、伸びない型を新型と入れ替える |
| `refresh.yml` | 毎月1日 | Threadsトークンを延命（失効防止） |
| `get-token.yml` | 手動（ボタン） | Threadsトークンを取得・保存（初回用） |
| `init-account.yml` | 手動（ボタン） | 前運用者のデータを空にする（初回用） |

> ⏰ **投稿時刻を変えるとき**は `config/slots.json` と `publish.yml` の `cron` の**両方**を直す
> （GitHubの時計はUTC。片方だけだとズレる）。Claudeに頼めば両方そろえてくれます。

---

## 4. 全ファイル解説

記号: ★＝よく触る（自分仕様の設定）/ ⚙️＝プログラム本体（普段触らない）/ 🔒＝自動更新（手で触らない）

### 🧠 `src/` — システムの本体（⚙️）
| ファイル | 役割 |
|---|---|
| `generate.py` | **下書き工場**。YouTube字幕＋型＋人格で連投を作り、CTAも付ける。文章の機械チェックもここ |
| `publish.py` | **投稿係**。連投を返信でつないで投稿。途中失敗でも次回続きから再開 |
| `measure.py` | 自分の投稿の成績（表示数・いいね等）を型ごとに集計 |
| `optimize.py` | 伸びない型を引退させ、新しい型をリサーチして入れ替える |
| `refresh_token.py` | トークンを延命し、GitHubの金庫(Secret)を更新 |
| `get_token.py` | トークンを取得する（初回セットアップ用） |
| `slots.py` | 「今どの投稿枠か」の時刻計算 |
| `threads_common.py` | 共通部品（設定読込・文字数カウント・通知・時刻処理） |

### 🔧 `scripts/` — 補助スクリプト
| ファイル | 役割 |
|---|---|
| `fetch_youtube.py` | YouTube人気動画選定・**字幕取得**（Apify経由。→ 9章） |
| `fetch_threads.py` | Threadsのバズ投稿収集（Apify経由。型リサーチ用） |
| `check_length.py` | 投稿の機械チェック（500字・記号など） |
| `init_account.py` | 新アカウント用にデータを空に戻す |
| `common.py` | scripts用の共通部品 |

### ★ `config/` — 設定（自分仕様にする所）
| ファイル | 役割 | 頻度 |
|---|---|---|
| `account.json` | ハンドル・表示名・ジャンル・誘導先・お手本・**CTAモード**・ピン留め投稿 | 高 |
| `slots.json` | 投稿する時刻・各枠のテーマ・トーン | 中 |
| `ng_words.json` | 使ってほしくない表現のリスト（生成時に回避＋検証で部分一致を弾く） | 低 |
| `account.example.json` | account.jsonの見本（触らない） | — |

> ✅ **`ng_words.json` は生成・検証に接続済み**（2026-09-11）。リストの語は生成プロンプトで「使わない」と
> 伝えたうえ、万一含まれたら `generate.py` の検証が部分一致で検知して作り直します。追記は
> `config/ng_words.json` に1行足すだけ。⚠️ ただし短すぎる語や普通の文に出がちな語を入れると
> 毎回引っかかって作り直しを繰り返すので、誇大・薬機法的な表現に絞ること。

### ★ `assets/` — キャラと文面（“味”を決める）
| ファイル | 役割 |
|---|---|
| `persona.md` | **キャラの人格設定（声・口調・性格）**。投稿の雰囲気そのもの。文章方針（絵文字なし等）もここ |
| `cta.md` | CTA（誘導文）の参考例 ※丸写しせず構成だけ参考にする |
| `youtube-channels.txt` | 素材にするYouTubeチャンネル（1行に1つ。追加・削除OK） |

### 📚 `kata-library/` — 型ライブラリ
| ファイル | 役割 |
|---|---|
| `<型名>.md` | 1ファイル＝1つの型（バズ構造の骨組み） |
| `INDEX.md` | 型の一覧表（`optimize`が自動更新） |
| `_common-modules.md` | 全投稿共通の文体・出力ルール（**絵文字なし・人間っぽく書く**もここ） |

> 型は `optimize.yml` が週1で自動入れ替え。引退した型は消えず `active: false` になるだけ。
> 手で足したいときはClaudeに「新しい型を足して」と頼む。

### 💾 `posts/` — 状態データ（🔒 自動で書く。手で触らない）
| ファイル | 役割 |
|---|---|
| `queue.json` | 下書き在庫（これから投稿する連投） |
| `state.json` | 各枠の投稿状況（二重投稿を防ぐ） |
| `processed.json` | 投稿済みID台帳（二重投稿を防ぐ） |
| `used_videos.json` | 使用済みYouTube動画（素材の重複を防ぐ） |
| `performance.json` | 型ごとの成績（**ここで成績が見られる**） |
| `token_meta.json` | トークンの有効期限メモ |

### 🆘 `fallback/` ＆ 📖 ドキュメント
| ファイル | 役割 |
|---|---|
| `fallback/evergreen.json` | 下書き生成が全部失敗したときに出す保険の定型投稿 |
| `SETUP.md` | ゼロから立ち上げる完全手順（初心者向け） |
| `システム解説.md` | ファイルの短い早見表（地図） |
| `システム完全ガイド.md` | このファイル（Webページ版の元） |
| `CLAUDE.md` | システム説明＋AI向け案内（Claudeが最初に読む） |
| `.env / .env.example` | ローカル実行用の鍵の見本。GitHubには上げない |
| `index.html` | このシステムと無関係の置き土産。無視/削除してOK |

---

## 5. 1日〜1週間で実際に起きること

```
【毎日 3:00】generate.py
  ・在庫が数日分を切っていたら、足りない枠だけ下書きを作る
  ・未使用の人気動画を選び字幕を取得（Apify）→ 型をローテーションで選びキャラの人格で執筆
  ・500字・文章ルール・重複なしを機械チェック（ダメなら最大3回作り直し）→ CTAを付けて在庫へ

【投稿枠ごと】publish.py（実際は前後の時間帯に15分おき実行）
  ・その枠の連投を在庫から取り出し1本ずつ投稿。2本目以降は前の投稿への返信＝連投
  ・1本ごとに記録。途中失敗でも次回続きから再開。最後の投稿にCTA

【毎週月曜 2:00】optimize.py
  ・measure.py が投稿の表示数・いいねを型ごとに集計 → performance.json
  ・十分たまった型のうち明らかに伸びない1つを引退（active:false）
  ・お手本アカウントのバズを調べ新しい型を1つ追加（1週1入れ替え・慎重）

【毎月 1日】refresh.py
  ・Threadsトークンを延命して、GitHubの金庫を更新（失効で止まるのを防ぐ）
```

---

## 6. 「〜したい」逆引き表（Claudeへの頼み方つき）

基本の直し方は **「Claude Codeで複製したリポジトリを開き、やりたいことを日本語で頼む」** だけ。
ClaudeがこのガイドとCLAUDE.mdを読んで正しいファイルを直し、GitHubへ反映します。

| やりたいこと | 触る場所 | 頼み方の例 |
|---|---|---|
| キャラの口調・性格を変える | `assets/persona.md` | 「キャラの口調をもっと〇〇に」 |
| ジャンル・誘導先・ハンドル | `config/account.json` | 「誘導先を新URLに変えて」 |
| 投稿の時刻を変える | `slots.json` ＋ `publish.yml` | 「投稿を8時・12時・20時に」 |
| 各枠のテーマ・トーン | `config/slots.json` | 「夜の枠をもっと〇〇に」 |
| 素材のYouTubeチャンネル | `youtube-channels.txt` | 「このチャンネルを追加して」 |
| CTA（誘導）の中身 | `generate.py` ＋ `cta.md` | 「誘導の言い方を変えて」 |
| **育成→販売（誘導強化）に切替** | `account.json` の `cta_mode` | 「販売モードに切り替えて」 |
| お手本アカウントを変える | `benchmark_accounts` | 「お手本に@〇〇を追加」 |
| 型を手で足す／止める | `kata-library/` | 「新しい型を足して」 |
| 最適化の慎重さ | `optimize.yml` の環境変数 | 「型の入れ替えをもっと慎重に」 |

---

## 7. 主要な設定と運用判断

### (A) CTAモード（誘導の強さ）
`config/account.json` の `cta_mode` で、投稿末尾の誘導（CTA）の方針が切り替わります。

- **`follow`（育成期）**：投稿の大半を「フォローして・保存して」という**フォロー獲得**に使い、
  数回に1回だけ商材（noteなど）へ誘導する。
- **`note`（販売期）**：毎回、商材（noteなど）へ誘導する。

**使い分け**：リーチ（表示数）は出るのにフォロワーがまだ少ない**立ち上げ期は `follow`** で
母数を増やし、フォロワーが十分育ってきたら（目安：おおむね150〜200人以上）**`note`（販売）**へ
切り替えます。Claudeに「販売モードに切り替えて」と頼めば、設定変更＋在庫の作り直しまでやってくれます。

### (B) 既定の文章方針：絵文字なし・人間っぽく
このテンプレートは既定で**絵文字を使わず、人間らしい文章を書く**方針です
（`persona.md`・`_common-modules.md`・`generate.py` の3か所で徹底＋自動チェック）。

**理由**：絵文字の多用や機械的なテンプレ文は「AIの量産投稿」に見えやすく、スパム判定でリーチが
落ちるリスクがあります。ジャンルに合わせて声（persona.md）は自由に変えてよいですが、
**絵文字なし・人間っぽさは維持することを推奨**します。

---

## 8. 鍵（GitHub Secrets）の一覧

GitHubの「Settings → Secrets and variables → Actions」に保管する鍵。**中身は誰にも見せない。**
各運用者が自分のぶんを用意します（取得手順は `SETUP.md`）。

| Secret名 | 用途 |
|---|---|
| `THREADS_USER_ID` | 投稿先のThreadsアカウントID |
| `THREADS_ACCESS_TOKEN` | Threadsへ投稿する鍵（約60日で失効→自動延命） |
| `THREADS_APP_ID` / `THREADS_APP_SECRET` / `THREADS_REDIRECT_URI` | トークン取得・延命に使う（Meta側アプリ情報） |
| `ANTHROPIC_API_KEY` | Claude（文章を書くAI）の鍵 |
| `YOUTUBE_API_KEY` | YouTube動画の検索に使う |
| `APIFY_TOKEN` | YouTube字幕取得・Threadsバズ収集に使う |
| `GH_PAT` | システムが自分でGitHubにファイルを書き戻す鍵 |
| `NOTIFY_WEBHOOK_URL` | （任意）失敗通知の送り先（Slack/Discord等） |

> トークンの有効期限は `posts/token_meta.json` に記録。毎月1日の `refresh.yml` が自動で延命するので
> 基本は放置でOKです。

---

## 9. ハマりどころ（これを知らないと詰まる）

仕様上の落とし穴。Claudeに直してもらう時の前提知識。

1. **連投（返信でつなぐ）には特別な権限が要る。**
   Threadsの返信投稿には `threads_manage_replies` 権限が必須。無いと返信作成が
   `HTTP 500 unknown error`（単発投稿は成功するので原因が分かりにくい）。トークンは権限付きで取得すること。

2. **YouTube字幕はGitHub上（クラウド）から直接取れない。**
   YouTubeがデータセンターのIPをブロックするため。なので **Apify経由**で取得（`APIFY_TOKEN`が必要）。
   「ローカルでは取れるのにGitHubだと全部失敗」はこれが原因。

3. **成績測定には別の権限が要る。**
   投稿のインサイト取得には `threads_manage_insights` 権限が必須。無いと権限エラー。
   トークン取得時にこの権限も付けること。

4. **文章ルールは自動で弾く。**
   生成文が500字超過・絵文字混入などの場合、`generate.py` の検証が検知して作り直す（安全網）。

5. **Threadsの一覧API（投稿直後）は反映が遅い。**
   投稿できたかの確認に使えない。確認は**Threadsアプリで目視**。

6. **GitHubへの書き戻しは競合に注意。**
   システムも自分でファイルを書き戻すので、手元から送るときは必ず
   「最新を取り込んでから送る（fetch→rebase→push）」。ここはClaudeがやってくれる。

---

## 10. トラブル対応（症状 → 対処）

まず見る場所は **GitHubの「Actions」タブ**（成功＝緑／失敗＝赤とログが見られる）。

| 症状 | 考えられる原因 | 対処 |
|---|---|---|
| 投稿が止まった | トークン失効／在庫切れ | Actionsで`publish`のログを見る。赤ならClaudeに渡す |
| 枠が「在庫切れ(missing)」 | generateが失敗して下書きが無い | `generate`のログ確認。字幕取得(Apify)失敗が多い |
| 変な投稿が出た | 方針変更前の在庫が残っていた | Threadsアプリで削除＋Claudeに「在庫を作り直して」 |
| 連投が返信でつながらない | 返信権限が無いトークン | トークンを権限付きで取り直す（`get-token.yml`） |
| 字幕が全部取れない | Apify残高切れ／トークン不正 | Apifyの利用状況を確認、鍵を再設定 |
| AIが書かない(生成ゼロ) | Anthropic鍵切れ／残高切れ | Anthropicの残高・鍵を確認 |
| トークンが切れそう | refreshが失敗していた | 手動で`refresh.yml`実行、ダメなら取り直す |

**原則：困ったら「GitHubのActionsの赤い実行のログ」をコピーしてClaudeに渡す。** 原因と直し方を教えてくれます。

---

## 11. 操作と変更のしかた

### 運用者が手を動かす数少ない操作
- **(a) 投稿を消す**：自動投稿は人のチェックを通らない。問題があれば**Threadsアプリ上で手で削除**
  （システムの状態には影響しない）。
- **(b) 様子を見る**：リポジトリの「**Actions**」タブ → 各実行履歴。緑＝成功／赤＝失敗。赤でログが読める。
- **(c) 手動実行**：Actionsタブで各ワークフローを選び「**Run workflow**」。`publish`はドライラン
  （投稿せず確認）も可能。
- **(d) 設定変更**：直接編集してもよいが、**基本はClaudeに日本語で頼むのが安全**。

### Claude Code を使った変更の流れ（技術メモ・任意）
- 変更前に必ず最新を取り込む：`git fetch` → `git rebase origin/main`（自動書き戻しがあるため競合防止）。
- **下書き在庫(queue.json)を作り直すとき**は、事故防止のため一時的に
  `publish.yml` / `generate.yml` / `optimize.yml` を**止めてから**作業し、終わったら**戻す**
  （GitHub APIでdisable→enable。Claudeが手順を知っている）。
- 変更を送ったら **必ずActionsのログと実際の投稿で結果を確認**（本番アカウント直結のため）。

---

## 12. 用語集

| 用語 | 意味 |
|---|---|
| テンプレート | 複製して自分用に設定するだけで動く"ひな形"。1複製＝1アカウント |
| 連投（スレッド） | 1本目に返信を重ねて続ける投稿の束。Threadsの基本形 |
| 型（kata） | バズ投稿から抜き出した"骨組み"。中身は素材から、構造だけ型から |
| CTA | Call To Action＝投稿末尾の誘導（フォローして／リンクを見て 等） |
| ペルソナ(persona) | キャラの人格・声の設定 |
| トークン | Threadsへ投稿する鍵。約60日で失効→毎月自動延命 |
| ベンチマーク | お手本にするThreadsアカウント。型リサーチの対象 |
| GitHub Actions | GitHubのクラウド上で決まった時刻にプログラムを走らせる仕組み |
| Secrets | GitHubに安全に保管する鍵（APIキー等） |
| Apify | Webからデータを取る外部サービス。字幕取得とバズ収集に使用 |
| queue（キュー） | 下書き在庫。これから投稿する連投のリスト |

---

## 困ったときの最終手段

1. **GitHubの「Actions」タブ**で赤い実行のログを見る → コピーしてClaudeに渡す
2. **Threadsアプリ**で投稿を目視・手で削除
3. **このガイドと `CLAUDE.md` をClaudeに読ませて**「〇〇したい／〇〇で困っている」と頼む

これだけ押さえれば、プログラミングが分からなくてもこのテンプレートを運用できます。
