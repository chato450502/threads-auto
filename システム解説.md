# システム解説 — リポジトリの中身と管理のしかた

このシステムを「理解して管理」するための解説です。どのファイルが何をしていて、
**何を変えたいときにどこを触ればいいか**が分かるようにまとめています。

> セットアップ手順そのものは `SETUP.md` を見てください。ここは「中身の地図」です。

---

## 1. このシステムは大きく3つの動きでできている

1. **毎日：下書きを作る → 決まった時刻に投稿する**（メインの自動投稿）
2. **週1：自分の投稿の成績を測り、伸びない「型」を新しい型と入れ替える**（自動最適化）
3. **型を作る／育てる**（②が自動でやる。人が対話AIで足すことも可能）

すべて **GitHub Actions（クラウドのタイマー）** が自動で動かします。あなたのパソコンは不要です。

---

## 2. いつ何が動くか（GitHub Actions ＝ `.github/workflows/`）

| ファイル | いつ | 何をする |
|---|---|---|
| `publish.yml` | 投稿時刻の前後（15分おき） | 予定時刻を過ぎた枠の連投を投稿する |
| `generate.yml` | 毎日 3:00（JST） | 3日分の連投下書きを作って在庫に貯める |
| `optimize.yml` | 毎週月曜 2:00（JST） | 成績を測り、伸びない型を新型と入れ替える |
| `refresh.yml` | 毎月1日 | Threadsトークンを延命（失効防止） |
| `get-token.yml` | 手動（ボタン） | Threadsトークンを取得・保存（初回セットアップ用） |
| `init-account.yml` | 手動（ボタン） | 前運用者のデータを空にする（初回セットアップ用） |

> ⏰ 投稿時刻を変えたいときは `config/slots.json` と `publish.yml` の `cron` の**両方**を直す。

---

## 3. フォルダ・ファイルの意味（★＝よく触る / ⚙️＝プログラム本体 / 🔒＝自動更新・触らない）

### 🧠 実行プログラム `src/`（システムの本体・⚙️）
| ファイル | 役割 |
|---|---|
| `generate.py` | ⚙️ 下書き生成。YouTube字幕＋型＋ペルソナで連投を作る。CTAも生成 |
| `publish.py` | ⚙️ 投稿本体。連投を返信で繋いで投稿。失敗時は再開・リトライ |
| `measure.py` | ⚙️ 自分の投稿の成績（表示・いいね）を型ごとに集計 |
| `optimize.py` | ⚙️ 成績で伸びない型を引退→新型をリサーチして入れ替え |
| `refresh_token.py` | ⚙️ トークン延命＋GitHub Secret更新 |
| `get_token.py` | ⚙️ トークン取得（認可URL表示→コード交換） |
| `slots.py` | ⚙️ 投稿枠の時刻計算 |
| `threads_common.py` | ⚙️ 共通処理（設定読込・文字数チェック・通知・時刻） |

> 基本、`src/` は仕組みそのもの。動きを変えたいとき以外は触りません。

### 🔧 補助スクリプト `scripts/`
| ファイル | 役割 |
|---|---|
| `fetch_youtube.py` | YouTubeの検索・字幕取得・チャンネルの人気動画選定 |
| `fetch_threads.py` | Threadsのバズ投稿収集（Apify経由。型リサーチ用） |
| `check_length.py` | 投稿の機械チェック（500字/記号など） |
| `init_account.py` | 新アカウント用にデータを空に戻す |
| `common.py` | scripts用の共通処理 |

### ★ 設定 `config/`（ここをよく触る）
| ファイル | 役割 | 触る頻度 |
|---|---|---|
| `account.json` | ★ ハンドル・表示名・ジャンル・note誘導先・ベンチマークアカウント | 高 |
| `slots.json` | ★ 投稿する時刻・テーマ・トーン | 中 |
| `ng_words.json` | ★ 使ってほしくない表現 | 低 |
| `account.example.json` | account.jsonの見本（触らない） | — |

### ★ キャラ・文面 `assets/`（ここが“味”を決める）
| ファイル | 役割 |
|---|---|
| `persona.md` | ★★ キャラクター設定（声・口調・性格）。投稿の人格そのもの |
| `cta.md` | ★ CTA（プロフ誘導）の参考例 |
| `youtube-channels.txt` | ★ 素材にするYouTubeチャンネル（1行1つ） |

### 📚 型ライブラリ `kata-library/`
| ファイル | 役割 |
|---|---|
| `<型slug>.md` | 1ファイル＝1つの「型（バズ構造の骨組み）」 |
| `INDEX.md` | 型の一覧表（`optimize`が自動更新） |
| `_common-modules.md` | 全投稿共通の文体・出力ルール |

> 型は `optimize.yml` が自動で入れ替えます。手で足すことも可能（対話AIに頼む）。
> 引退した型は消えず `active: false` になるだけ。

### 💾 状態データ `posts/`（🔒 システムが自動で書き換える。基本触らない）
| ファイル | 役割 |
|---|---|
| `queue.json` | 🔒 下書き在庫（これから投稿する連投） |
| `state.json` | 🔒 各枠の投稿状況（二重投稿防止） |
| `processed.json` | 🔒 投稿済みID台帳（二重投稿防止） |
| `used_videos.json` | 🔒 使用済みYouTube動画（素材の重複防止） |
| `performance.json` | 🔒 型ごとの成績（`measure`が出力） |
| `token_meta.json` | 🔒 トークンの有効期限メモ |

### 🆘 予備 `fallback/`
| ファイル | 役割 |
|---|---|
| `evergreen.json` | 生成が全部失敗したときの“保険”の定型投稿 |

### 📖 ドキュメント・設定
| ファイル | 役割 |
|---|---|
| `SETUP.md` | セットアップ完全手順（初心者向け） |
| `CLAUDE.md` | システム説明＋AIアシスタント向けの案内 |
| `README.md` | 自動投稿システムの概要 |
| `システム解説.md` | このファイル |
| `requirements.txt` | 必要なPythonライブラリ一覧（GitHubが自動で入れる） |
| `.env.example` | 環境変数の見本 |
| `.env` | 🔒 APIキー（自分のPC内だけ。GitHubには上がらない） |
| `.gitignore` | GitHubに上げないファイルの指定 |
| `.claude/skills/` | 対話AI用の作業手順（kata-register/write/list）。自動投稿には未使用 |
| `index.html` | ※ このシステムとは無関係の置き土産ファイル。無視/削除してOK |

---

## 4. データの流れ（1日〜1週間で何が起きるか）

```
【毎日3:00】generate.py
  YouTube字幕 ＋ 型(kata-library) ＋ persona.md
     → 連投の下書きを作成 → posts/queue.json に貯める

【9:00 / 15:00 / 21:00】publish.py
  queue.json から今の枠の連投を取り出す
     → Threadsに投稿（返信で連結） → state.json に「投稿済み」を記録

【毎週月曜2:00】optimize.py
  measure.py が投稿の成績を集計 → posts/performance.json
     → 伸びない型を active:false（引退）
     → ベンチマークからバズをリサーチ → 新しい型を kata-library に追加

【毎月1日】refresh.py
  トークンを延命して Secret を更新
```

---

## 5. 「〜を変えたい」→ どこを触る（管理者向け早見表）

| やりたいこと | 触る場所 |
|---|---|
| キャラの口調・性格を変える | `assets/persona.md` |
| 発信ジャンル・note誘導先・ハンドルを変える | `config/account.json` |
| 投稿の時刻を変える | `config/slots.json` ＋ `publish.yml` の `cron`（両方） |
| 投稿枠ごとのテーマ・トーンを変える | `config/slots.json` |
| 素材にするYouTubeチャンネルを変える | `assets/youtube-channels.txt` |
| CTAの誘導の型を変える | `assets/cta.md`＋`src/generate.py` のCTA指示 |
| リサーチ先（お手本アカウント）を変える | `config/account.json` の `benchmark_accounts` |
| 使ってほしくない言葉を足す | `config/ng_words.json` |
| 型を手で追加/削除したい | `kata-library/` に型ファイルを追加、または `active: false` に |
| 最適化の慎重さを変える | `optimize.yml` の環境変数（OPT_MIN_SAMPLES等） |
| 生成・投稿のロジックを変える | `src/generate.py` / `src/publish.py`（要注意・本体） |

---

## 6. 触ると危ないもの・自動で変わるもの

- **`posts/` の中身**：システムが自動で書き換える“状態”。手で編集すると二重投稿や
  投稿漏れの原因になるので基本さわらない（`init-account.yml` で空にするのはOK）。
- **`.env` と GitHub Secrets**：APIキー。他人に見せない。`.env` はGitHubに上がらない設定済み。
- **`src/` の本体コード**：変えると全体の挙動が変わる。変更したら必ず、実際の投稿と
  GitHubの「Actions」ログで結果を確認する（本番アカウントに直結するため）。

---

## 7. 迷ったら

- **中身を変えたい** → 対話AI（Claude Code）にこのリポジトリを開いて「〇〇を変えたい」と頼む。
  このファイルと `CLAUDE.md` を読んで、正しい場所を直してくれる。
- **動きがおかしい** → GitHubの「Actions」タブで、失敗した実行のログを見る。
- **成績を見たい** → `posts/performance.json`（型ごとの平均表示・いいね）。
